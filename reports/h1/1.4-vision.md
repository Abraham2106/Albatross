# 1.4 Prueba mínima visual QVAC

Fecha: 2026-09-10. Responsable: Inferencia (esta sesión). Estado: **Terminado**.

## Criterio

Imagen produce salida real; modelo, API, versión, memoria y tiempos registrados. Sin mock ni nube.

## Ejecución

1. Fixture ficticia `fixtures/h1/placa-sintetica.png` (40 899 bytes, SHA-256 `a6f22a1e3db50bc12b9d9c03639484d0101ffe4df2ed78ef5940fdc4b2a2a38c`).
2. Primer intento: `RPC_INIT_TIMEOUT` a 30 s por lock viejo en `%USERPROFILE%\.qvac\.worker.lock` (PID 40048 muerto). Lock eliminado; `heartbeat` posterior en 20 658 ms.
3. `node --experimental-strip-types --no-warnings scripts/h1-vision-probe.mjs` — OK.

## Contrato usado (`@qvac/sdk@0.18.2`)

- `loadModel({ modelSrc: VISIONPSY_NANO_460M_MULTIMODAL_Q4_K_M, modelConfig: { projectionModelSrc: MMPROJ_VISIONPSY_NANO_460M_MULTIMODAL_Q8_0, image_no_upscale: 'on', device: 'gpu', main-gpu: 'dedicated', split-mode: 'none', ctx_size: 2048 } })`
- `completion({ attachments: [{ path }], responseFormat: json_schema, generationParams: { temp: 0, seed: 42 } })`
- Pesos: VisionPsy Nano 460M **Flash** Q4_K_M (303 143 488 B) + mmproj Q8_0 (108 782 144 B). Descarga vía registro QVAC (no `models/` del producto).

## Resultado

| Campo | Esperado | Modelo |
| --- | --- | --- |
| brand | BluePeak Medical | BluePeak Medical |
| model | Aether 3T | Aether 3T |
| modality | Magnetic Resonance | Magnetic Resonance |
| serial | BP-A3T-88421 | **BP-ABT-88421** (confunde `3` con `B`) |
| manufactureDate | 2017-03 | 2017-03 |

Salida real, no simulada. Un error de carácter en la serie es evidencia de calidad, no de fallo de arranque.

## Recursos y tiempos

| Medida | Valor |
| --- | --- |
| GPU seleccionada | NVIDIA GeForce RTX 2050 (`GGML_VK_VISIBLE_DEVICES` índice 1) |
| `stats.backendDevice` | `gpu` |
| Carga (incl. descarga registro) | 35 207 ms |
| Inferencia | 35 342 ms |
| TTFT | 32 839 ms |
| Tokens/s | 79.4 |
| Prompt / emitidos | 511 / 93 |
| RAM sistema tras inferir | 15 663 951 872 / 16 329 850 880 B (~96 % usados) |
| Total del probe | 78 682 ms |

La RAM de 16 GiB queda al límite con el worker + VisionPsy. Confirma que no se puede convivir con Whisper F16 y Qwen 4B a la vez; la cola secuencial del producto es obligatoria.

## Artefactos

- `reports/h1/1.4-vision.json` (traza completa, `getSystemResources` antes/después)
- `scripts/h1-vision-probe.mjs`
- `fixtures/h1/placa-sintetica.png` y `placa-sintetica.json`

## Límites

- Placa sintética nítida; no sustituye el dataset de 5.4.
- Serie no es coincidencia exacta; abstención vs invención queda para 5.2.
- El worker Bare en este Windows necesita lock limpio; el timeout interno del SDK es 30 s y un arranque frío ya consume ~20 s.
